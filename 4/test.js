/**
 * @type {import('markdown-it/lib/parser_inline').RuleInline}
 */
const testing = (a, b) => false;

// Bad: `RuleInline` shows as unresolved variable or type (VSCode automatically pulls it from the correct types library)
